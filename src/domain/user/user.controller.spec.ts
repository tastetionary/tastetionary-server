//TODO: remove test once user controller is implemented
describe('dummy test ', () => {
  it('dummy test', () => {
    const expected = 2;
    const result = 1 + 1;
    expect(result).toEqual(expected);
  });
});
